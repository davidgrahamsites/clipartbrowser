# STATUS

The watcher (or any agent that detects a conflict) writes here. Agents **read
this before claiming a task**: if the latest entry is `CONFLICT`, STOP and resolve
with the lead before continuing.

CLEAR · 2026-06-20 · setup — coordination layer initialized.
